# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/

# Secrets — never commit
config/secrets.yaml

# Test artifacts
reports/
.tmp/

# App binaries (stored externally / uploaded to BrowserStack) — large, never commit
apps/*.apk
apps/*.ipa
apps/*.aab
mobile_apk/
*.apk
*.ipa
*.aab

# IDE
.idea/
.vscode/

# (reports/ is already ignored above — covers Extent-styled report.html + run-log.json + screenshots)

# Local working artifacts — defect/Jira scratch JSONs at repo root
.defect_*.json
.phix*.json
.phix_*.json

# Tool state
.playwright-mcp/

# Demo deck deps — vendored node_modules should never be committed
demo/node_modules/
