# ─── MobileAutoQA — Secrets Template ──────────────────────────────────────────
# Copy this to secrets.yaml and fill in real values.
# secrets.yaml is GITIGNORED — never commit real credentials.

browserstack:
  username: puneet125
  access_key: ""               # your BrowserStack access key

credentials:
  prod:
    employee_password: ""        # PROD employee password
  qa:
    employee_password: ""

reporting:
  email:
    from_addr: ""                # sender email
    password: ""                 # SMTP app password (Gmail app password, not account password)
  google_chat:
    webhook_url: ""              # https://chat.googleapis.com/v1/spaces/.../messages?key=...&token=...
